name: flutter_demo_01
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  percent_indicator: ^4.2.3

  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_svg: ^2.0.9
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
      - assets/image/
      - assets/image/2.0x/
      - assets/image/3.0x/
      - assets/icons/

